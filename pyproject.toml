[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "diffusers>=0.35.2",
    "fastapi>=0.119.0",
    "pydantic>=2.12.3",
    "torch==2.8.0",
    "transformers>=4.57.1",
]
